import { TimesPE3Directive } from './times-pe3.directive';

describe('TimesPE3Directive', () => {
  it('should create an instance', () => {
    const directive = new TimesPE3Directive();
    expect(directive).toBeTruthy();
  });
});
